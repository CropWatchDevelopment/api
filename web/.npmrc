# CWUI is published to GitHub Packages. The auth token comes from your ~/.npmrc
# (or a NODE_AUTH_TOKEN/CI secret); this only pins the scope to the registry.
@cropwatchdevelopment:registry=https://npm.pkg.github.com
